hall of fame rpg [한국어 ~~발번역~~]

Copyright (C) TEKITO PROJECT for JUICE.

Modded by https://github.com/Remileon/hof

PHP8 version by Kustale.

Used translator : translator.google.com, PAPAGO

돌리는 방법 :

웹서버 루트나 폴더를 만들어서 코드를 통째로 넣고 폴더에 chmod . 0755를 주면 됨.

나머지는 ... 몰?루

php8 버전 -> 대부분? 고쳐짐

= 소스 코드 주석 있는거 고치는데 방해되서 싹다 지우고 쪼개져 있던 코드 약간 줄이면서 합체함.

한국어 버전 -> 작업 완료(중문 같은 일문이라 번역 상태가 이상함)

작업 내용 : (다 php 5.6에서 뭘 다 바꿔놧는지...)

작업 전에 주석 달아 놓은거 거치적 거려서 전부 지워버림(없어도 되잖애... ~~에??~~)

$_GET $_POST 에서 워닝이(php 5.6부터 난리) 잔뜩 생겨서 따로 만들어 둠 -> __GET, __POST ...

(isset이라는 방법을 썼는데 요새 php 버전이 올라가면서 ?? 같은 null coalescing operator을 isset이랑 같이 쓰면 코드 줄이고 좋으나 혹시나 php5 쓸지 몰라서 코드가 좀 길어져도 삼항 연산자 남발함...

그래서 겁나 오래된 서버에서도 잘 돌아감... 은 장담 못함 ~~어?~~)

count에 값이 없을때 워닝 뜨는거 수정 -> __count (위의 $_GET 문제랑 같음)

class/에 있는 코드들 function에 같은 이름이 들어 있는거 php7에서 오류(php8은 컷)를 뿜는데 마침 __construct 방법이 있어서 전부 수정

중간에 php 코드 쪼개진거 적절히 print를 붙여서 합체 -> print <<< + 여기에 약간 모딩 (참고로, 쪼개진 코드 중간에 함정이 몇몇 숨어 있었는데 그것도 다 고침)

변수에 [""]이런거 없을때 오류 나는거 일일이 "추가함

ereg -> preg_match

코드 길다고 울어대서 줄여줌.

중문, 일문 있는거 번역 완성 (번역기 딸깍)

몇몇 오류 뜨는거 수정중

user, union, log폴더랑 얻어온 git에 기록되어있던 유저 정보들 전부 제거 + 폴더 없을때 오류 뜨기 전에 생성 코드 추가

유저 정보 폴더에 접근을 일절 못하도록 index.php를 다 추가. (했더니 버그도 추가되서 다 수정)

github에 올릴때 dat파일들 중요하지 않으므로 모두 삭제 + 파일이 없을때 오류 뜨는거 전부 수정 (루트의 dat파일들은 캐릭터 생성이나 작동시 생성됨)

전투 잘 되는지는 모르지만 고쳐짐

현재 안되는것 : 아직 못 찾은 무언가... (거의 다 고쳐졌을거라 봄)
